# Inittab syntax:
#
#	VARIABLE=value
#
# 	code   name   command arg arg arg ...

# Environment
PATH=/bin:/sbin:/usr/bin
LOCALE=C

# System initialization
mount	w	/sbin/mount -o remount,rw /
syslog	v	/sbin/syslogd -n
clock	r	/sbin/hwlock -s

# VTs
su	s1	/sbin/sulogin
tty1	s	/sbin/agetty --noclear -8 -s 38400 tty1 linux
tty2	s	/sbin/agetty -8 -s 38400 tty2 linux
tty3	s	/sbin/agetty -8 -s 38400 tty3 linux
tty4	s	/sbin/agetty -8 -s 38400 tty4 linux
tty5	s	/sbin/agetty -8 -s 38400 tty5 linux
tty6	s	/sbin/agetty -8 -s 38400 tty6 linux

# Shutdown
umount	r0	/sbin/mount -o remount,ro /
